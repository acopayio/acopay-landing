/**
 * iOS status on `/download`.
 *
 * Kevin 2026-08-18: 1.0.240 (build 107) is on TestFlight.
 * App Store / Review remains HOLD. No public join URL in repo.
 */
export const IOS_APP = {
  version: "1.0.240",
  build: "107",
  channel: "testflight",
} as const;
